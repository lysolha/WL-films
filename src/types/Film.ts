export type Film = {
  id: string;
  title: string;
  year: string;
  rating: string;
  format: string;
  list: string[];
};

export type FormFilm = {
  title: string;
  year: string;
  rating: string;
  format: string;
  list: string[];
};
